# Day 17: Mock CLI Response Interceptor
Learn about Python regex matching, `str.strip()` on command strings, why whitespace causes `re.match` to fail, and the mock CLI interceptor pattern.
Task: Fix regex matching to strip whitespace before matching.
Instruction: Run `bats track-atomic/day-17/test.bats`.
