# Day 18: Antigravity Workspace Manifest Generation
Learn about Python json.JSONEncoder custom subclass, datetime serialization, why datetime objects are not JSON-serializable by default, and isoformat().
Task: Fix the generator to use a custom encoder that serializes datetime as an ISO 8601 string.
Instruction: Run `bats track-atomic/day-18/test.bats`.
